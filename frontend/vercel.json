{
  "outputDirectory": "build",
  "buildCommand": "CI=false pnpm run build",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "https://backend.filevault.hariomop.in/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}